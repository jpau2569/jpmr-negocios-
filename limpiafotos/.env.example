# Copia este archivo como .env  (o define la variable en tu hosting).
# Consigue tu clave en: https://clipdrop.co/apis  ->  API Keys
CLIPDROP_API_KEY=pon_aqui_tu_clave

# Opcional: puerto en el que escucha el servidor (por defecto 3000).
# PORT=3000
